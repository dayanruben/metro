<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2025 Zac Sweers -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="dev.zacsweers.metro.benchmark.startup.android.benchmark" />
    </queries>

    <application
        android:name=".BenchmarkApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <profileable
            android:shell="true"
            android:enabled="true" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <!-- Use BenchmarkApplication's TraceDriver when adb flushes traces. -->
            <meta-data
                android:name="androidx.tracing.profiler.ConnectedProfilerTracingInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
